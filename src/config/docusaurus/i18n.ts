import type { I18nConfig } from "@docusaurus/types";
import { DeepPartial } from 'utility-types';

const i18n: DeepPartial<I18nConfig> = {
  defaultLocale: 'en',
  locales: ['en'],
};

export default i18n;
